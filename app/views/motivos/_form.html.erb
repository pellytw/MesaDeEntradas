<%= form_for(@motivo) do |f| %>
  <% if @motivo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@motivo.errors.count, "error") %> prohibited this motivo from being saved:</h2>

      <ul>
      <% @motivo.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nombre %><br />
    <%= f.text_field :nombre %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
