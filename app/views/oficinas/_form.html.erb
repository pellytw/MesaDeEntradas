<%= form_for(@oficina) do |f| %>
  <% if @oficina.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@oficina.errors.count, "error") %> prohibited this oficina from being saved:</h2>

      <ul>
      <% @oficina.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :organismo_id %><br />
    <%= collection_select(:oficina, :organismo_id, Organismo.all, :id, :nombre) %>
  </div>
  <div class="field">
    <%= f.label :localidad_id %><br />
    <%= collection_select(:oficina, :localidad_id, Localidad.all, :id, :nombre) %>
  </div>
  <div class="field">
    <%= f.label :nombre %><br />
    <%= f.text_field :nombre %>
  </div>
  <div class="field">
    <%= f.label :descripcion %><br />
    <%= f.text_area :descripcion %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
